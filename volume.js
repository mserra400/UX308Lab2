// 3) Write a program to *calculate* the *volume* of a *cylinder*. Start with a variable that contains the **radius** of the end of the circle, and another variable that references the **height** in meters.

let radius = 5;
let height = 3;
let volume = 3.14 * radius ** 2 * height;

console.log(`${radius} radius is ${height} height is ${volume} volume`);